# OpenGL Renderer Module

The Kotlin OpenGL renderer sources live under `app/src/main/java/com/example/vision/gl`.
This directory documents the renderer portion of the architecture and can host shared GLSL
shaders or renderer-specific assets if you wish to extract them from the main Android module.

